const mythemekey = "myTheme";
const appFont = 'Poppins';
const String petDb = "USER_DB";

enum FontStyle {
  heading,
  heading2,
  subheading,
  title,
  subtitle,
  boldTitle,
  boldSubtitle,
  appbarTitle,
  h14,
  h16
}

enum AppState {
  inital,
  loading,
  success,
  error,
}
